# 发布 2026.9.12.1 —— 稳健性与可用性一轮

> 方案:`2026-09-12-robustness-and-usability-optimization-plan.md`;实施计划:`2026-09-12-robustness-and-usability-impl-plan.md`;
> PR openxlings/xlings#591;配套 openxlings/xim-pkgindex#826。
> 状态:**已发布,§4 为真机数字。**

## 1. 一句话

四条痛点是三个结构性原因的四张脸(§0 of the plan);这一版把 P0–P5 全部落地,外加 #590,
以及整分支 review 之后的一波(F1–F11)。

## 2. 交付(按用户能看到的)

见 PR #591 描述。新增 e2e:E2E-100 … E2E-110(11 条);单测 54 套;`unit-asan` 成为 Linux CI 硬门禁。

## 3. 实施中被推翻的判断(接方案 §8)

1. **「删掉了但装不回来」退出 0 可以沿用** —— 整分支 review 判为 Critical,推翻:spec 的 I2/I3(退出 0 ⇒ 可信)优先于旧语义。
2. **E2E-104 I5 用只读 subos 让子进程失败** —— 合并 Task 5 的 `--force` 梯子后不再确定性失败;换成不安全命名的 subos 触发守卫。
3. **「全 home 扫描慢」** —— 量出来默认模式 5.9 s 已含 111 个 subos 快照;贵的是 `--fix --dry-run` 的 36 s。
4. **`local:` 堆积和 mcpp 构建流程有关** —— 三个 mcpp 工作树里零处 `--add-xpkg`;来源是 pkgindex 的 recipe 测试循环。
5. **`set_active_subos_override` 可用于安装侧刷新** —— 它的 `reload_state()` 会清掉未保存的内存 DB 批次;改为直接读 pinning subos 的 workspace 文件。
6. **第二 subos 安装已装载荷会拒绝**(Task 7 发现)—— 在最终 HEAD 上不复现(Task 5 的 DB 优先解析顺带修了),留了回归守卫 E2E-107。
7. **两个互相抵消的 bug 让 self_doctor_multi_subos S6 一直绿**:`cmd_remove` 剥离顺序让 `--force` 静默 no-op,`repair_incomplete_` 用 PATH 上的 `xlings`;修完之后 S6 要重写成断言真实性质。

## 4. 发布后验证(真机 `~/.xlings`,111 subos)

| 检查 | 方法 | 结果 |
|---|---|---|
| GitHub 资产 | `gh release view v2026.9.12.1` | **8** 个(4 归档 + 4 sidecar) |
| GitCode 镜像 | 本地 `tools/mirror-latest.sh xlings`(CI 的 mirror 段不等);linux-x86_64 第一次上传未通过,重跑一次通过 | 8/8 OK;linux-x86_64 用 GET 拉回:200,sha256 与 GitHub sidecar、索引条目三方一致 |
| 索引 `latest` | xim-pkgindex#830(`version-check.py --apply --only xlings`,三张平台表 + latest) | 全部 `2026.9.12.1`;publish-artifact `7da7d99` |
| 索引快照传播 | 发布期快照 `2026.9.12.1`(14:28)早于 bump 合入(14:46),`xlings index use xim latest` + `xlings update` 后拿到 `7da7d99` | 与上一版相同的既有滞后,不是本次引入 |
| `self update` | 真实 home 2026.9.5.1 → **2026.9.12.1**(`xlings --version` 跑出来) | ok;旧二进制最后仍印了一次旧的 nudge(它是旧代码),新客户端不再印 |
| 提示 I1 | pty 下 `xlings list` 两遍 | 第一遍 1 行升级提示,第二遍 0;`hintsSeen` 里有 `client.upgraded` 与 `catalog.demoted…mcpp` |
| doctor D1 | `self doctor`:说"没有索引提供"的条目 | **100 → 3**(与 `--deep` 一致);6.7 s;给出 102 条 `xlings install` remedy |
| doctor D2 / 跨 subos | `self doctor --fix --dry-run`,30 s | **prune 103,install 7,子进程 `--subos` 26**(改前:99 次下载,0 prune,38 条要用户先 `subos use`) |
| 覆盖层 | `config --list-xpkg` → `xlings update` → 再 list | 167 条(161 identical)→ **删 161** → 剩 5(mcpp/xlings 落后一版,3 个 unique) |
| 生态(`xlings subos use <name> --sandbox --cmd …`,mirror = CN) | default:`xlings 2026.9.12.1`、gcc 16.1.0、Python 3.13.12;dev-hello:gcc 15.1.0;gfxbuild:199 个库、GL/vulkan 在位、gcc 可用;eco-vk-test:vulkan 在位;cortexm-verify:PATH 路由正常(7z/adb…) | ok ×5 subos |
| `self doctor` in default | 退出 1:110 条 broken payload(真实 home 的历史欠账,`--fix` 的计划见上) | 预期;**没有在真实 home 跑 `--fix`**(会 prune 103 条注册、进 26 个 subos 重装),留给维护者决定 |

**没跑完的:** `.agents/tools/verify-release-lifecycle.sh` 在隔离 home 里两次卡在 `self init` 的 GitHub 索引 clone(30 分钟无进展,本机网络);发布工作流的 candidate-*/fresh-install 作业覆盖了同一面。

**一处待改的文案:** 报告里 ForeignPayload 的逐条 remedy 仍是 `xlings subos use X && xlings self doctor --fix`(38 条),应改成 `xlings self doctor --fix --subos X`;汇总行已经说 `--fix` 会自己去修。

## 5. 发布后开的 issue

- openxlings/xlings#592 —— Windows 上新 bash e2e 的 ps1 移植
- openxlings/xlings#593 —— interface/capabilities 对新动词的对等支持
- openxlings/xlings#594 —— `XLINGS_LOCK_TIMEOUT` 恢复时置空而非取消设置
